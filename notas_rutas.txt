/api/v1/admin

    .get("/colegios" )          lista de colegios
    .get("/colegio/:id" )       colegio x ID
    .post("/colegio" )          guardar colegio

    .get("/sedes/:idColegio" )  sedes x ID de colegio
    .get("/sede/:id" )          sede x ID
    .post("/sede" )             guardar sede

    .post("/docentes" )         guardar docentes CSV
    .post("/docente" )          guardar docente

    .post("/director")          asignar Director a Sede

/api/v1/director
    .post("/tutor/convivencia") asignar TutorConvivencia a Sede
    .post("/psicologo")         asignar Psicologo a Sede

    .post("grupo")              crear Grupo en Sede

    .post("/alumno" )           guardar alumno
    .post("/alumnos" )          guardar alumnos CSV


/api/v1/app
    .get("/docentes/:idSede" )  docentes x sedeID
    .get("/docente/:id" )       docente x ID

    .get("/grupos/:idSede")     Grupos x Sede

    .get("/alumnos/:idGrupo" )  alumnos x Grupo
    .get("/alumno/:id" )        alumno x ID
    .get("/alumno/dni/:dni" )   alumno x Dni

    .get("/incidencias/:idSede" )   incidencias x Sede
    .post("/incidencia" )           guardar incidencias